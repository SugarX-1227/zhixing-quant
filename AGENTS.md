# AGENTS.md

给在本仓库工作的 AI 助手的指引。本文件优先级高于助手的通用习惯。

---

## 0. 这个项目是什么

A 股盘后选股 + 回测系统。行情数据来自**本地通达信文件**，不走任何付费 API。

```
通达信 vipdoc/**/*.day  (32 字节定长二进制)
        │  增量读取（记录字节偏移，只读新增部分）
        ▼
data/market.db  (SQLite, 主键 code+trade_date)
        │
        ├─→ zhixing_quant/scanner/   选股
        ├─→ zhixing_quant/backtest/  回测
        └─→ app.py                   Streamlit Web 界面
```

必须知道的几件事：

- **数据是不复权的。** `.day` 文件存的是原始价格。除权除息事件通过 pytdx 拉取存进
  `xdxr` 表，`load_daily(adjust="qfq")` 才会做前复权。没有除权数据时会静默退化为不复权，
  用 `df.attrs["adjust"]` 判断实际口径。
- **`sh000001` 是上证指数，`sz000001` 是平安银行。** 6 位代码会撞车。指数在库里用
  `市场+代码` 作 key，个股用 6 位。写任何涉及代码的逻辑前先确认这一点。
- **结果不落文件。** 不要再生成 HTML/CSV 报告，一切走 Web 界面或终端输出。
- **`.gitignore` 里必须是 `/data/` 而不是 `data/`。** 后者会连
  `zhixing_quant/data/` 源码目录一起忽略掉——这个仓库历史上真的因此丢失过整个数据访问层。

---

## 1. 交付标准：跑过才算完成

**这是本仓库最重要的一条，因为历史上违反最多的就是它。**

「代码写完了 + 测试全绿」不等于完成。曾经的实际情况：

- `backtest/engine.py` 有完整的 dataclass、类型注解、docstring，唯独没有
  `import pandas`，`run()` 一调用就 NameError。它从未被执行过，因为
  85 个测试里没有一个碰过 `BacktestEngine.run()`。
- B2 扫描器只要求 3 根 K 线就开始计算，但指标内部要算 MA114。
  pandas 的 `min_periods=1` 让它不报错地返回垃圾数值。
- `config/settings.yaml` 里 `timing` 和 `portfolio` 各写了两遍。
  YAML 后者覆盖前者，改上面那份永远不生效，而且不会有任何提示。
- `sell` 整段配置从没被 `signals/sell_s.py` 读过，阈值硬编码在函数体里；
  同一个文件里 S1 的「跌破前低」用了含当日的 rolling min，
  条件在数学上恒不成立，那条分支一次都没跑过。
- 砖型图判据被改成比两个水平值而非两个增量，信号量从每年数百次掉到
  2.8 年 1 次。测试全绿，因为测试锁的就是错的那版。

**加一条检查动作**：改了判据之后，统计一下它在一段真实/合成行情上的
触发频率，和文档里的预期量级对一下。数量级不对就是改错了，
这比逐行读公式快得多。

这些的共同点：**从代码外观完全看不出问题，只有真的跑一遍才会暴露。**

所以：

- 改了某条代码路径，就要**实际执行它一次**并把输出贴出来。不能只跑 `pytest` 就交差。
- 新增模块的测试必须覆盖到**主入口函数**，不能只测辅助函数。
- 没有真实数据时，自己构造合成数据跑通。`tests/test_tdx_local_data.py`
  里有生成 `.day` 二进制文件的现成例子，照抄即可。
- 说不出「我跑了什么、输出是什么」的改动，视为未完成。

---

## 2. 量化领域的硬规则

这些错误不会让程序崩溃，只会让回测好看、实盘亏钱。写任何涉及信号或回测的代码前必读。

**未来函数（look-ahead bias）**
信号在 T 日收盘后才产生，所以最早只能在 T+1 开盘成交。任何用当日收盘价成交、
用了 `shift(-n)`、或在计算 T 日指标时读到 T 日之后数据的写法，都是错的。
回测结果异常漂亮时，第一个要怀疑的就是这个。

**复权**
在原始价格上算均线、KDJ、砖型图，除权日附近必然产生假信号。实测过：一次 10 送 10
在不复权数据里表现为 **-49.3% 的单日暴跌**，前复权后还原成 +1.5% 的正常交易日。
新增任何指标时，确认它拿到的是 `adjust="qfq"` 的数据。

**T+1 与涨跌停**
当日买入当日不可卖。一字涨停买不进，一字跌停卖不出。回测省略这些会系统性高估收益。
主板 10%，创业板/科创板 20%。

**跳空穿越**
止损挂在 9.50、次日直接低开到 8.00 时，实盘只能在 8.00 附近成交。
按触发价记账等于假设每次跳空都能在缺口上沿接住，而且跳得越狠虚增越多——
恰好在最该扣分的那几笔上给了加分。实测把这一条修对，b1 的总收益从
+26.4% 变成 -6.1%，最大回撤从 10.2% 变成 18.5%。
见 `backtest/engine.py::_intraday_exit`。

**出场规则不许写死在引擎里**
止损、移动止损、止盈、时间止损全部走 `backtest/exits.py` 的 ExitPolicy，
配置在 `settings.yaml` 的 `exits` 段。回测和实盘读同一份。
在引擎里直接写死一个 0.15 止盈，就是把回测和实盘再劈成两套系统。

**仓位口径**
回测默认走 `portfolio/PositionSizer` 的风险头寸公式（`backtest.sizing: risk`），
和实盘下单计划同一个函数。单票上限与择时总仓位上限一律从
`portfolio/sizer.py::per_position_cap / regime_cap` 取，不要再在别处写一份。

**交易成本**
佣金（有最低 5 元）、印花税（仅卖出）、过户费、滑点，一个都不能少。
A 股必须 100 股整手（科创板 200 股以上可按 1 股递增）。

**因子层的时点正确性**
横截面标准化只能在**单个交易日内**做。跨日期做 z-score 需要全样本的均值方差，
那是未来信息。`factors/evaluate.py` 是全项目唯一允许 `shift(-n)` 的地方
（forward_returns 是被解释变量），它的输出绝不能进信号或排序。
新增因子后跑一遍 `tests/test_factors.py::test_no_factor_reads_the_future`——
它截断后半段数据，要求前半段的因子值逐位不变。

**幸存者偏差**
股票池来自当前库内数据，退市股不在里面。这个限制目前没解决，
别在结论里假装解决了。

---

## 3. 想清楚再动手

- 明确说出你的假设。不确定就问，不要静默选一个。
- 有多种解读时全部列出来，不要替用户做决定。
- 有更简单的方案就说出来，该反对就反对。
- 看不懂的地方停下来，指出具体哪里不清楚，然后问。

---

## 4. 简单优先

- 不写超出需求的功能。
- 单次使用的代码不要做抽象。
- 不加没人要求的「灵活性」和配置项。
- 不为不可能发生的情况写错误处理。
- 写了 200 行但 50 行能解决，就重写。

判断标准：一个资深工程师会不会觉得这过度设计了？

**例外**：同一段逻辑已经复制粘贴三份以上时，抽取公共部分不算过度设计，是在还债。
（`scanner/_core.py` 就是这么来的——三个扫描器原本 95% 相同。）

---

## 5. 外科手术式修改

- 不「顺手改进」相邻的代码、注释、格式。
- 不重构没坏的东西。
- 匹配现有风格，哪怕你有别的偏好。
- 发现无关的死代码，**说出来**，不要直接删。

**但是**：本仓库经历过多次不同 AI 的改造，存在不少「看起来对但跑起来是错的」代码。
如果你在做手头任务时确认某处**确实有 bug**（不是风格问题，是运行会出错的问题），
把它写进报告，并明确区分：

- 「这是我为了完成任务必须修的」
- 「这是我顺带发现的，没动，你来决定」

不要因为「surgical」就假装没看见真 bug，也不要不打招呼就大范围重写。

修改产生的孤儿（因你的改动而不再被引用的 import、变量、函数）要清理掉。
改动之前就存在的死代码不要动。

---

## 6. 目标驱动执行

把任务转成可验证的目标：

- 「加个校验」→「先写非法输入的测试，再让它通过」
- 「修 bug」→「先写能复现的测试，再让它通过」
- 「重构 X」→「确保重构前后测试都通过」

多步任务先给简短计划：

```
1. [做什么] → 验证：[怎么确认做对了]
2. [做什么] → 验证：[怎么确认做对了]
```

---

## 7. 常用命令

```bash
# 每日增量同步（几秒）
python -m zhixing_quant.data.sync

# 首次建库 / 补名称和除权除息
python -m zhixing_quant.data.sync --full --names --xdxr

# 命令行选股
python -m zhixing_quant.scanner.daily_brick
python -m zhixing_quant.scanner.daily_b1 --date 20250115 --limit-universe 300

# Web 界面
streamlit run app.py

# 测试
python -m pytest -q
```

---

**这份指引起作用的标志**：diff 里没有无关改动；不会出现写完从没跑过的代码；
澄清性问题出现在动手之前，而不是搞砸之后。
